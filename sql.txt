----CREATE DATABASE COMMAND-------
CREATE DATABASE PAYROLL_SERVICE;

---CREATE TABLE COMMAND-----

CREATE TABLE EMPLOYEE_PAYROLL 
(
    ID INT IDENTITY(1,1),
    NAME VARCHAR(255),
    SALARY INT ,
    START_DATE DATE
)

SELECT * FROM EMPLOYEE_PAYROLL;

SELECT * FROM EMPLOYEE_PAYROLL WHERE NAME='AKHIL';

---ADDING COLUMN GENDER-----
ALTER TABLE EMPLOYEE_PAYROLL ADD  GENDER CHAR;

---SUM OF SALARY OF FEMALE EMPLOYEES----
SELECT SUM(SALARY) AS TOTAL_SALARY FROM EMPLOYEE_PAYROLL WHERE EMPLOYEE_PAYROLL.GENDER='M'

---EXTEND TABLE TO EMPLOYEE INFORMATION LIKE EMPLOYEE_PHONE,ADDRESS AND DEPARTMENT
ALTER TABLE EMPLOYEE_PAYROLL ADD PHONE INT , ADDRESS VARCHAR(255) DEFAULT 'GURGAON' , DEPARTMENT VARCHAR(255);

ALTER TABLE EMPLOYEE_PAYROLL ADD BASIC_PAY INT , DEDUCTIONS INT, TAXABLE_PAY INT , INCOME_TAX INT , NET_PAY INT;